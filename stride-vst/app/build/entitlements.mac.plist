<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Electron/V8 JIT needs writable executable memory -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Node.js native modules + Electron's V8 snapshots -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Electron requires DYLD_* environment variables to start its helpers -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>

    <!-- Allow loading non-Apple-signed libraries (Node native modules,
         third-party frameworks bundled inside the Electron app) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
